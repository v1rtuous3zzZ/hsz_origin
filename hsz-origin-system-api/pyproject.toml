[project]
name = "hsz-origin-system-api"
version = "1.0.0"
requires-python = ">=3.11"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501", "E701"]

[tool.pytest.ini_options]
testpaths = ["tests"]
